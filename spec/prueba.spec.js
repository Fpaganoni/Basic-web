describe("Un string con la descripción del grupo de tests que queremos englobar.", function () { //Un callback que agrupa los diferentes tests unitarios que serán necesarios.
  it("Este test debe pasar siempre", function () {
    expect(4 + 2).toBe(6);
  });
});
